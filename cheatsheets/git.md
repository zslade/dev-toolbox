# Git Cheatsheet

## Daily shortcuts

```bash
git status
git add -A
git commit -m "message"
git pull --rebase
git push
```

## Useful history/log

```bash
git log --oneline --graph --decorate -20
git diff --staged
git reflog
```
